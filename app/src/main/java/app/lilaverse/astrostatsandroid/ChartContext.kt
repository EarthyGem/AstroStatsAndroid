package app.lilaverse.astrostatsandroid.chat

enum class ChartContextType {
    NATAL,
    TRANSIT,
    SYNASTRY,
    RELOCATED,
    OTHER_PERSON
}

enum class ChartTimeContext {
    PAST,
    PRESENT,
    FUTURE,
    TIMELESS
}